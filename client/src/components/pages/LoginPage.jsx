import LoginForm from "../auth/LoginForm";

const LoginPage = () => {
    return (
        <div style={{ backgroundColor: 'var(--old-lace)' }}>
            <LoginForm />
        </div>
    );
};

export default LoginPage;